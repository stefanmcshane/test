"on":
  push:
    branches:
    - main
name: Bash Test
jobs:
  porter-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      run: |
        echo $OSTYPE
    - name: Install Porter CLI
      run: |
        /bin/bash -c "$(curl -fsSL https://install.porter.run)"
        echo Stefan - $(porter version)
        
  porter-deploy-ubuntu-old:
    runs-on: ubuntu-20.04
    steps:
    - name: Checkout code
      run: |
        echo $OSTYPE
    - name: Install Porter CLI
      run: |
        /bin/bash -c "$(curl -fsSL https://install.porter.run)"
        echo old - $(porter version)        
