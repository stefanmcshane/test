"on":
  push:
    branches:
    - main
name: Bash Test
jobs:
  porter-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      run: |
        echo $OSTYPE
    - name: Install Porter CLI
      run: |
        /bin/bash -c "$(curl -fsSL https://install.porter.run)"
        porter version
